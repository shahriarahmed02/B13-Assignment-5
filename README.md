 ***Create a readme file ,
 and answer this question on your own. Don’t copy-paste from Google or any AI chatbot.*** 
 
    - 1️⃣ What is the difference between var, let, and const?
   **Ans= Var is a old function-scoped can be declare many times with same name where Let and const are blocked scoped. we cant change the value of const but we can change the value of Let . 
    
    - 2️⃣ What is the spread operator (...)?
    **Ans= Its a one kind of copy method are mainly used to separate elements from array or object.
    
    - 3️⃣ What is the difference between map(), filter(), and forEach()?
    **Ans=Map() returns a new array with modified values and Filter() also returns only array which one is pass through condition where foreach just a loop which donot return any array.

    - 4️⃣ What is an arrow function?
    **Ans= Its a Shorter and easy method to write a function 
   Ex: const greet = () => "Hello";

    - 5️⃣ What are template literals?
**Ans: Its a method to write string with using bacticks(`) By this method we can push variable by using ${} directly in string.




